import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(<App/>,
    document.getElementById('root')
);

// In line No.5, we should put two parameter such as "<App/>" and "document.getElemntById('root') " into "ReactDOM.render();"