import React from 'react';
import Todos from './components/Todos';

const App = () => {
  return(
      <Todos/>
      // Basically, in "App.js" file, we should put tags included links of derectries which relevant to each components like "./components".
  );
}

export default App;
